interface:
  display_name: "Multi-Angle Product Identity Lock Board"
  short_description: "Standalone six-view board with an explicit verified worker."
  default_prompt: "Use $multi-angle-product-identity-lock-board directly with my product references to request one horizontal 16:9 six-view board; delegate the terminal image call to exactly one non-decision worker, inspect the bound board in the main agent, and publish the complete exact final_generation_prompt plus generation_prompt_sha256 and the complete image-specific final_4k_enhancement_prompt plus 4k_enhancement_prompt_sha256 in the same task's final main result. Treat any downstream AI-video project handoff as optional and external to this Skill."
policy:
  allow_implicit_invocation: false
