interface:
  display_name: "AI Video Timed Animatic / Previs Director"
  short_description: "Build timing animatics and provider-bound control previs."
  default_prompt: "Use $ai-video-timed-animatic-previs-director with the supplied Shot and Storyboard artifacts to create provider-neutral V1 timing. Create optional V2 control previs only when exact P1, K2, keyframe, and provider-capability artifacts are supplied; never require their producer packages."
policy:
  allow_implicit_invocation: true
