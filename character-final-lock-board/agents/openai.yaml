interface:
  display_name: "Character Final Lock Board"
  short_description: "Generate final character lock boards."
  default_prompt: "Use $character-final-lock-board with my model reference and wardrobe reference to generate one final character lock board image plus QA."
policy:
  allow_implicit_invocation: true
