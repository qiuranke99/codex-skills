interface:
  display_name: "Character Final Lock Board"
  short_description: "16:9 character lock with verified 4K handoff."
  default_prompt: "Use $character-final-lock-board with my references to generate one exact 16:9 character board, disclose its bound generation prompt, then inspect it and deliver a source-bound Nano Banana 4K enhancement prompt and handoff."
policy:
  allow_implicit_invocation: true
