interface:
  display_name: "Character Final Lock Board"
  short_description: "Horizontal character lock with a verified 4K prompt pair."
  default_prompt: "Use $character-final-lock-board with my references to request one horizontal 16:9 character board. End the generation turn at imagegen; on the later continuation inspect the real board, then publish in the later final main result the complete exact final_generation_prompt plus generation_prompt_sha256 and the complete image-specific final_4k_enhancement_prompt plus 4k_enhancement_prompt_sha256."
policy:
  allow_implicit_invocation: true
