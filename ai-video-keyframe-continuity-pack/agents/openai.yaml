interface:
  display_name: "AI Video Keyframe Continuity Pack"
  short_description: "Generation-ready Omni anchors and state continuity ledgers."
  default_prompt: "Use $ai-video-keyframe-continuity-pack after the shot contract, final modular storyboard, canon assets, global look, and V1 timing are approved. Create K1 Omni-reference anchors and state ledgers before unit preflight, then create a separate K2 boundary supplement after the exact P1 plan. Never use first/last-frame mode."
policy:
  allow_implicit_invocation: true
