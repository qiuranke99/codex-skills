interface:
  display_name: "Character Casting Lock Board"
  short_description: "Per-board 16:9 casting locks with 4K handoffs."
  default_prompt: "Use $character-casting-lock-board with my selected references to generate exact 16:9 casting boards, disclose each bound generation prompt, then inspect every generated board and deliver its own source-bound Nano Banana 4K prompt and handoff."
policy:
  allow_implicit_invocation: true
