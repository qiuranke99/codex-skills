interface:
  display_name: "Single-Face Character Lock Board"
  short_description: "One-face 16:9 lock with source-bound 4K handoff."
  default_prompt: "Use $single-face-character-lock-board with my references to generate the exact 16:9 one-face topology, disclose its bound generation prompt, then inspect it and deliver a source-bound Nano Banana 4K enhancement prompt and handoff."
policy:
  allow_implicit_invocation: true
