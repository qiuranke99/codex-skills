interface:
  display_name: "Single-Face Character Lock Board"
  short_description: "One-face horizontal lock with a verified 4K prompt pair."
  default_prompt: "Use $single-face-character-lock-board with my references to request the horizontal 16:9 one-face topology. End the generation turn at imagegen; on the later continuation inspect the real board, then publish in the later final main result the complete exact final_generation_prompt plus generation_prompt_sha256 and the complete image-specific final_4k_enhancement_prompt plus 4k_enhancement_prompt_sha256."
policy:
  allow_implicit_invocation: true
