interface:
  display_name: "Single-Face Character Lock Board"
  short_description: "One-face board with source-bound visual QA and prompt pairs."
  default_prompt: "Use $single-face-character-lock-board directly with my references to request the horizontal 16:9 one-face topology. Use the available image tool directly, inspect the actual returned board, and publish the complete exact final_generation_prompt plus generation_prompt_sha256 and the complete image-specific final_4k_enhancement_prompt plus 4k_enhancement_prompt_sha256 in the same task's final main result. Treat any downstream AI-video project handoff as optional and external to this Skill."
policy:
  allow_implicit_invocation: false
