interface:
  display_name: "AI Visual Director"
  short_description: "Director workflow for reference analysis, product-faithful storyboards, image prompts, and Google Omni video segments."
  default_prompt: "Run the AI Visual Director workflow for these references and keywords. Produce reference roles, route decision, shot plan, product identity lock when needed, storyboard sheet prompts, Google Omni segment prompts, validation results, and final QC artifact paths."
