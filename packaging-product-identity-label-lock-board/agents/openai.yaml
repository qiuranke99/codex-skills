interface:
  display_name: "Packaging Exact-Copy Rotation Asset Pack"
  short_description: "OCR-first exact labels plus rotation-ready independent product masters."
  default_prompt: "Use $packaging-product-identity-label-lock-board to OCR every supplied product image before generation, freeze the exact-copy SSOT and code/graphic evidence, derive the legal R profile plus high/low/top/bottom, four-way upper/lower bridge views, frozen detail semantics, and one exact-copy detail per physical region, generate one independent horizontal 16:9 master per view, apply product copy deterministically, verify final-pixel readability/OCR/decode/continuity, then build canonical ordered semantic review boards only from approved masters."
