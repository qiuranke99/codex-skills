{
  "type": "object",
  "required": ["product_name", "mapping_items", "unsupported_or_missing"],
  "properties": {
    "product_name": {"type": "string"},
    "mapping_items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["mapping_id", "reference_beat_id", "product_role", "product_claims", "source_refs", "claim_status"],
        "properties": {
          "mapping_id": {"type": "string"},
          "reference_beat_id": {"type": "string"},
          "product_role": {"type": "string"},
          "product_claims": {"type": "array"},
          "source_refs": {"type": "array"},
          "claim_status": {"type": "string", "enum": ["provided", "inferred", "needs_confirmation", "not_applicable"]},
          "adaptation_note": {"type": "string"}
        }
      }
    },
    "unsupported_or_missing": {"type": "array"}
  }
}
